# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/ms_backend?schema=public"

# CORS Configuration
# Option 1: Single origin (legacy support)
# CORS_ORIGIN=https://yourdomain.com

# Option 2: Multiple origins (comma-separated)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,http://localhost:3000,http://localhost:3001

# Server Configuration
PORT=7300
NODE_ENV=development

# Optional: JWT Secret (if using authentication)
# JWT_SECRET=your-super-secret-jwt-key
